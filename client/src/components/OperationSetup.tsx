import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import { BellRing, Plus, ShoppingBasket } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

type Product = { id: number; name: string; description: string | null; category: string; price: string | number; available: boolean; sortOrder: number };
type Connection = { database: string; hardware: { state: string; queued: number } };
const emptyProduct = { id: undefined as number | undefined, name: "", description: "", category: "Cachorro-Quente", price: "", available: true };

export default function OperationSetup({ products, connection, refresh }: { products: Product[]; connection: Connection | undefined; refresh: () => void; settings?: unknown; sponsors?: unknown; events?: unknown }) {
  const [product, setProduct] = useState(emptyProduct);
  const saveProduct = trpc.operations.saveProduct.useMutation({ onSuccess: () => { toast.success("Produto salvo."); setProduct(emptyProduct); refresh(); }, onError: error => toast.error(error.message) });
  const connect = trpc.connectivity.connectHardware.useMutation({ onSuccess: () => { toast.info("Verificação de conexão iniciada."); refresh(); }, onError: error => toast.error(error.message) });
  const test = trpc.connectivity.testHardware.useMutation({ onSuccess: () => { toast.success("Teste de sirene enviado."); refresh(); }, onError: error => toast.error(error.message) });
  const submit = () => saveProduct.mutate({ id: product.id, name: product.name, description: product.description || undefined, category: product.category, price: Number(product.price), available: product.available });
  const selectProduct = (item: Product) => setProduct({ id: item.id, name: item.name, description: item.description ?? "", category: item.category, price: String(item.price), available: item.available });
  return <section><div className="mb-6"><p className="text-sm font-bold uppercase tracking-[.16em] text-orange-800">Primeiro passo</p><h1 className="font-display text-3xl font-bold">Cadastre, conecte e comece a vender.</h1></div><div className="grid gap-6 2xl:grid-cols-[1.1fr_.9fr]"><div className="rounded-[2rem] border border-stone-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl font-bold">Produtos</h2><p className="mt-1 text-sm text-stone-600">O catálogo é a fonte de verdade do pedido.</p></div><Button onClick={() => setProduct(emptyProduct)} className="rounded-xl bg-[#9c321e] font-bold hover:bg-[#7f2819]"><Plus className="mr-1 h-4 w-4" />Novo</Button></div><div className="mt-5 max-h-64 overflow-y-auto rounded-2xl border border-stone-100">{products.length ? <div className="divide-y divide-stone-100">{products.map(item => <button key={item.id} onClick={() => selectProduct(item)} className="flex w-full items-center gap-4 px-4 py-4 text-left hover:bg-stone-50"><div className="grid h-10 w-10 place-items-center rounded-xl bg-orange-50 text-[#9c321e]"><ShoppingBasket className="h-4 w-4" /></div><div className="min-w-0 flex-1"><p className="font-bold text-stone-900">{item.name}</p><p className="truncate text-xs text-stone-500">{item.category}</p></div><p className="font-bold text-stone-800">R$ {Number(item.price).toFixed(2).replace(".", ",")}</p></button>)}</div> : <div className="p-8 text-center text-sm text-stone-500">Nenhum produto cadastrado.</div>}</div><div className="mt-5 grid gap-3"><div><Label htmlFor="product-name">Nome</Label><Input id="product-name" className="mt-1" value={product.name} onChange={event => setProduct(current => ({ ...current, name: event.target.value }))} placeholder="Ex.: Cachorro-quente" /></div><div className="grid gap-3 sm:grid-cols-2"><div><Label htmlFor="product-category">Categoria</Label><Input id="product-category" className="mt-1" value={product.category} onChange={event => setProduct(current => ({ ...current, category: event.target.value }))} placeholder="Ex.: Lanches" /></div><div><Label htmlFor="product-price">Preço</Label><Input id="product-price" type="number" min="0" step="0.01" className="mt-1" value={product.price} onChange={event => setProduct(current => ({ ...current, price: event.target.value }))} placeholder="0,00" /></div></div><div><Label htmlFor="product-description">Descrição</Label><Input id="product-description" className="mt-1" value={product.description} onChange={event => setProduct(current => ({ ...current, description: event.target.value }))} placeholder="Descrição opcional" /></div><div className="flex items-center justify-between rounded-xl bg-stone-50 p-3"><Label htmlFor="product-available" className="font-bold">Disponível no pedido</Label><Switch id="product-available" checked={product.available} onCheckedChange={checked => setProduct(current => ({ ...current, available: checked }))} /></div><Button disabled={!product.name || !product.category || !product.price || saveProduct.isPending} onClick={submit} className="rounded-xl bg-[#9c321e] hover:bg-[#7f2819]">{saveProduct.isPending ? "Salvando…" : product.id ? "Atualizar produto" : "Salvar produto"}</Button></div></div><div className="rounded-[2rem] border border-stone-200 bg-white p-6 shadow-sm"><p className="text-xs font-extrabold uppercase tracking-[.14em] text-stone-500">Segundo passo</p><h2 className="font-display mt-1 text-2xl font-bold">Conexão do hardware</h2><p className="mt-2 text-sm leading-6 text-stone-600">Teste a comunicação com LEDs e sirene antes de abrir a barraca. O pedido continua funcionando se o controlador estiver desligado.</p><div className="mt-5 rounded-2xl bg-stone-50 p-4 text-sm text-stone-700"><div className="flex items-center justify-between"><span>Banco de dados</span><strong className={connection?.database === "ONLINE" ? "text-emerald-700" : "text-rose-700"}>{connection?.database ?? "OFFLINE"}</strong></div><div className="mt-3 flex items-center justify-between"><span>Arduino</span><strong className={connection?.hardware.state === "ONLINE" ? "text-emerald-700" : "text-rose-700"}>{connection?.hardware.state ?? "OFFLINE"}</strong></div><p className="mt-3 text-xs text-stone-500">Comandos na fila: {connection?.hardware.queued ?? 0}</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Button disabled={connect.isPending} onClick={() => connect.mutate()} variant="outline" className="h-12 rounded-xl">{connect.isPending ? "Conectando…" : "Verificar conexão"}</Button><Button disabled={test.isPending} onClick={() => test.mutate()} className="h-12 rounded-xl bg-[#9c321e] hover:bg-[#7f2819]"><BellRing className="mr-2 h-4 w-4" />Testar sirene</Button></div></div></div></section>;
}
